# =============================================================================
# Verses - Environment Variables
# =============================================================================
# Copy this file to .env and customize for your environment:
#   cp .env.example .env
#
# For development on archbook or similar local machines, you can use the
# example values below. For production, generate secure passwords and keys!
# =============================================================================

# -----------------------------------------------------------------------------
# MySQL Database
# -----------------------------------------------------------------------------

# Root password for MySQL administrator access
# Generate a secure one for production: openssl rand -base64 32
MYSQL_ROOT_PASSWORD=verses_root_secret_change_me

# Password for the 'verses_user' application user
# This is used by FastAPI to connect to the database
MYSQL_PASSWORD=verses_db_secret_change_me

# -----------------------------------------------------------------------------
# FastAPI Backend
# -----------------------------------------------------------------------------

# Secret key for JWT token signing (CRITICAL for security!)
# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=change_me_generate_a_real_secret_key

# Debug mode (True/False)
# Enable for development (verbose errors), disable in production
DEBUG=True

# -----------------------------------------------------------------------------
# Derived Variables (auto-constructed by docker-compose.yml)
# -----------------------------------------------------------------------------
# These are built from the variables above, no need to set them manually:
#
# DATABASE_URL=mysql+pymysql://verses_user:${MYSQL_PASSWORD}@db:3306/verses
# MYSQL_DATABASE=verses (hardcoded)
# MYSQL_USER=verses_user (hardcoded)
# -----------------------------------------------------------------------------
