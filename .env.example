# =============================================================================
# Verses - Environment Variables
# =============================================================================
# Copy this file to .env and customize for your environment:
#   cp .env.example .env
#
# For development on archbook or similar local machines, you can use the
# example values below. For production, generate secure passwords and keys!
# =============================================================================

# -----------------------------------------------------------------------------
# Environment Configuration
# -----------------------------------------------------------------------------

# Environment type: development | production
# - development: HTTP only, localhost, no SSL certificates required
# - production: HTTPS with SSL, real domain, Let's Encrypt certificates
VERSES_ENVIRONMENT=development

# Path to SSL certificates (only used in production)
# - development: ./nginx/certs (empty placeholder)
# - production: /etc/letsencrypt
VERSES_CERTS_PATH=./nginx/certs

# -----------------------------------------------------------------------------
# MySQL Database
# -----------------------------------------------------------------------------

# Root password for MySQL administrator access
# Generate a secure one for production: openssl rand -base64 32
VERSES_MYSQL_ROOT_PASSWORD=verses_root_secret_change_me

# Password for the 'verses_user' application user
# This is used by FastAPI to connect to the database
VERSES_MYSQL_PASSWORD=verses_db_secret_change_me

# -----------------------------------------------------------------------------
# FastAPI Backend
# -----------------------------------------------------------------------------

# Secret key for JWT token signing (CRITICAL for security!)
# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
VERSES_SECRET_KEY=change_me_generate_a_real_secret_key

# Debug mode (True/False)
# Enable for development (verbose errors), disable in production
VERSES_DEBUG=True

# -----------------------------------------------------------------------------
# Derived Variables (auto-constructed by docker-compose.yml)
# -----------------------------------------------------------------------------
# These are built from the variables above, no need to set them manually:
#
# DATABASE_URL=mysql+pymysql://verses_user:${MYSQL_PASSWORD}@db:3306/verses
# MYSQL_DATABASE=verses (hardcoded)
# MYSQL_USER=verses_user (hardcoded)
# -----------------------------------------------------------------------------

# -----------------------------------------------------------------------------
# Production Example
# -----------------------------------------------------------------------------
# For production deployment, use these values:
#
# VERSES_ENVIRONMENT=production
# VERSES_CERTS_PATH=/etc/letsencrypt
# VERSES_DEBUG=False
# VERSES_MYSQL_ROOT_PASSWORD=<generate with: openssl rand -base64 32>
# VERSES_MYSQL_PASSWORD=<generate with: openssl rand -base64 32>
# VERSES_SECRET_KEY=<generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))">
# -----------------------------------------------------------------------------
